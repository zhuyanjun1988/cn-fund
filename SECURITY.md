# 安全与敏感信息

本仓库是只读投资研究代码，不应包含账户、券商接口、交易权限、cookies、token、
私钥或个人持仓。

如果发现凭据、个人数据、可绕过限购/风控的实现，或代码具有意外下单能力，请勿
在公开 Issue 中粘贴敏感内容；应通过 GitHub 仓库所有者的私有联系方式报告。

安全边界：

- 网络适配器只采集公开研究数据；
- 所有输出仅是模拟研究，不连接真实账户；
- 未知、冲突、陈旧或缺失的产品状态应失败关闭；
- `.gitignore` 不是秘密存储，凭据必须保存在系统钥匙串或受控密钥服务；
- `data/`、`raw/`、`runtime/` 和本地数据库不进入版本控制。
